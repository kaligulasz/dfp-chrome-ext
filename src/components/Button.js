import React from 'react';
import styled from 'styled-components';

const ButtonWrapper = styled.button`
  background: red;
`

const Button = () => (
  <ButtonWrapper>Jakis button</ButtonWrapper>
)

export default Button;